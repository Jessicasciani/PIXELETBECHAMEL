<div class="banner-photos" style="background-image: url( <%= image_path "header-photo.jpg" %>);">
  <h1>PORTFOLIO</h1>
  <div class="trait-souligne">
  </div>
</div>

<div class="container">
  <div class="row">

    <% @folders&.each do |f| %>
      <div class="col-xs-12 col-md-4">
        <% if f.photo? %>
          <div class="card-client" style="background-image: url('<%=  cl_image_path f.photo %>'); background-size: cover;background-position: center;">
        <% else %>
          <div class="card-client" style="background-color: grey;">
        <% end %>
          <%= link_to folder_portfolio_photos_path(f), class: "link-folder" do %>
            <h1><%= f.client %></h1>
          <% end %>
        </div>
        <% if current_user&.admin %>
          <%= link_to "X", folder_path(f), method: :delete  %>
          <%= link_to "Edit", edit_folder_path(f)  %>
        <% end %>
      </div>
    <% end %>
  </div>



  <% if current_user&.admin  %>
    <div class="form-cache">
      <%= render 'folders/form', folder: @folder %>
      <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
    </div>
  <% end %>

</div>
