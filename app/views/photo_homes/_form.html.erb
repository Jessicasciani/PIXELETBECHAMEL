<%= simple_form_for(photo_home) do |f| %>
  <%= f.input :photo, class: "form-control" %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'Go', class: "abtn abtn-lg btn-primary" %>
<% end %>
