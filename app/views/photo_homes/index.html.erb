<div id="myCarousel" class="carousel slide carousel-fade" data-ride="carousel">
  <!-- Indicators -->



    <ol class="carousel-indicators">
      <% @photo_homes.each_with_index do |photo, index| %>
        <li data-target="#myCarousel" data-slide-to="<%= index %>" class="<%= 'active' if index == 0 %>"></li>
      <% end %>
    </ol>

  <!-- Wrapper for slides -->
  <div class="carousel-inner">
    <div class="item active">
      <%= image_tag @photo_homes[0].photo  %>
    </div>
    <% @photo_homes[1..5].each do |photo_home| %>
      <div class="item">
        <%= image_tag photo_home.photo %>
      </div>
    <% end %>
  </div>
  <a class="left carousel-control" href="#myCarousel" data-slide="prev">
    <span class="glyphicon glyphicon-chevron-left"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="right carousel-control" href="#myCarousel" data-slide="next">
    <span class="glyphicon glyphicon-chevron-right"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<% if current_user&.admin %>
<div class="formulaires">
  <div class="row">
    <div class="col-xs-12 col-md-2">
      <p>Ajouter une photo</p>
      <div class="form-home">
        <%= render 'photo_homes/form', photo_home: @photo_home %>
        <%= link_to "Sign out", destroy_user_session_path, method: :delete %>
      </div>
    </div>
      <% @photo_homes.each_with_index do |photo_home, index| %>
        <div class="col-xs-12 col-md-2">
          <p>Modifier photo <%= index + 1%></p>
          <div class="form-home">
            <%= render 'photo_homes/form', photo_home: photo_home, method: :patch%>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% end %>

<div class="container">
  <div class="waste flex">
    <%= image_tag "waste.jpg" %>
    <p> Le studio de photographies culinaires Pixel et Béchamel met en place des actions quotidiennes visant à réduire les déchets alimentaires et à les recycler.
    Cette gestion inclut la distribution, via l’application To Good To Go, de certains plats réalisés sur place. Nous reversons tous les revenus de ces transactions à l'association Action contre la faim.</p>
  </div>

    <% if @articles&.last != nil && @articles.last&.photo? %>
      <div class="article flex">
        <%= cl_image_tag @articles.last.photo %>
        <div class="article-text">
          <h2 class="title-article"><%= @articles.last.title %></h2>
          <p><%= @articles.last.created_at.strftime("%b %d, %Y") %></p>
          <p><%= @articles.last.text %></p>
          <p> <%= link_to "Plus d'articles...", articles_path  %></p>
        </div>
      </div>
    <% end %>


       <p class="contact-btn"><%= link_to "Contactez-nous", contact_path %></p>


</div>


<%= content_for(:title, "Welcome") %>
