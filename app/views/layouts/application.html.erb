<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= content_for?(:title) ? yield(:title) : "Pixel et Béchamel" %></title>
    <meta name="description" content="Pixel et Béchamel">

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="Pixel et Béchamel" />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="<% asset_path 'cappucino.png' %>">
    <meta property="og:description" content="Pixel et Béchamel">
    <meta property="og:site_name" content="Pixel et Béchamel"/>

    <%= favicon_link_tag 'logo.png' %>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>

    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
  </head>
  <body>
      <%= render 'shared/header' %>
    <div id="wrapper">
      <%= render 'shared/navbar' %>
    </div>
    <div class="layout-content">
      <%= yield %>
    </div>
    <%= render 'shared/footer' %>
    <%= javascript_include_tag 'application' %>
  </body>
</html>
