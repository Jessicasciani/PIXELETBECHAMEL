<%= simple_form_for(portfolio_photo) do |f| %>
  <%= f.input :client, class: "form-control" %>
  <%= f.input :photo, class: "form-control" %>
  <%= f.input :photo_cache, as: :hidden %>
  <%= f.submit 'Ajouter une photo', class: "abtn abtn-lg btn-primary" %>
<% end %>
